# Jainam Lite API Credentials
# Copy this file to .env and fill in your actual credentials

# ============================================================
# REQUIRED: Static credentials (get from Jainam Developer Portal)
# ============================================================

# User ID - Your Jainam trading account user ID
JAINAM_USER_ID=your_user_id

# API Secret - Your vendor API secret key
JAINAM_API_SECRET=your_api_secret

# App Code - Your registered application code
JAINAM_APP_CODE=your_app_code

# ============================================================
# OPTIONAL: For automated testing only
# ============================================================

# Password - For automated browser login tests (NOT recommended for production)
JAINAM_USER_PASSWORD=

# TOTP Secret - For automated TOTP generation in tests
TOTP_TOKEN=

# ============================================================
# NOTE: authCode is NOT stored in .env
# ============================================================
# authCode is one-time use and changes every login.
# Use the SessionManager to:
# 1. Input authCode after browser login
# 2. Cache the session token in ~/.jainam_session.json
# 3. Reuse the cached session until it expires
#
# Example:
#   from jainam_api_client import interactive_login
#   session = interactive_login()  # Prompts for authCode if needed
#   api = session.get_api_client()
# ============================================================
